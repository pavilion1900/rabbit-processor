spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  cloud:
    stream:
      function:
        definition: processor
      bindings:
        processor-in-0:
          destination: sample.queue
          group: processor
        processor-out-0:
          destination: sample.queue2
          group: consumer

server:
  port: 8081
